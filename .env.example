# Private key for the facilitator wallet (needs Base Sepolia ETH for gas)
# Generate with: cast wallet new   or   node -e "console.log('0x'+require('crypto').randomBytes(32).toString('hex'))"
FACILITATOR_PRIVATE_KEY=0x...

# Address that receives USDC from /api/quote payments
# Defaults to facilitator's own address if left unset — can be a separate wallet
PAYTO_ADDRESS=0x...

# Base Sepolia RPC URL (optional — defaults to the public https://sepolia.base.org endpoint)
# Use a private RPC for production to avoid rate limits:
#   Alchemy:   https://base-sepolia.g.alchemy.com/v2/<key>
#   Infura:    https://base-sepolia.infura.io/v3/<key>
#   QuickNode: https://<slug>.base-sepolia.quiknode.pro/<key>
# RPC_URL=https://...
